apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://netmind-prometheus:9090
    isDefault: true
    editable: true
    version: 1
    jsonData:
      timeInterval: "5s"
      queryTimeout: "30s"
      httpMethod: POST

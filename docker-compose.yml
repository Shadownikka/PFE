version: '3.8'

services:
  netmind:
    build: .
    container_name: netmind-app
    network_mode: host
    cap_add:
      - NET_ADMIN
      - NET_RAW
    privileged: true
    volumes:
      - .:/app
    stdin_open: true
    tty: true
    restart: unless-stopped

# NetMind — Native Desktop App Dependencies
# Install: pip install -r requirements.txt

# ── Networking ───────────────────────────────
scapy>=2.5.0
netifaces>=0.11.0
termcolor>=2.4.0

# ── AI / LLM ─────────────────────────────────
ollama>=0.1.8

# ── Voice (optional — experimental feature) ─
SpeechRecognition>=3.10.0
# PyAudio is installed via system package manager (python3-pyaudio)
# to avoid portaudio.h build errors. Do not add it here.

# ── Metrics ──────────────────────────────────
prometheus-client>=0.19.0

# ── Desktop UI ───────────────────────────────
PyQt6>=6.6.0
PyQt6-Qt6>=6.6.0
